\documentclass[crop, tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes, arrows}
\begin{document}
% Define block styles
\tikzstyle{decision} = [diamond, draw, fill=blue!20, 
    text width=4.5em, text badly centered, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, fill=blue!20, 
    text width=9em, text centered, rounded corners, minimum height=4em]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{terminal} = [draw, ellipse,fill=red!20, node distance=3cm,
    minimum height=2em]
\tikzstyle{while_block} = [trapezium, draw, minimum width=3cm,
trapezium left angle=120, trapezium right angle=60, ]
\begin{figure}[htpb]
\begin{center}
\begin{tikzpicture}[scale=1, transform shape]
    \node[terminal](start){Start DOAS algorithm}; 
    \node[block](init){
            \begin{table}
                \centering
                \begin{tabular}{c}
                    $s = s_i$\\
                    $\delta = k$\\
                    $\mathbf{x}=LSQ(\tau', A)$\\
                    $\chi^2=chisq(\tau', \mathbf{X})$\\
                    $\chi^2_{old} = \chi^2$\\
                \end{tabular}
            \end{table}
        }; 
    \node[while_block](while){while criteria is True};
    \node[block](iterate){
            \begin{table}
                \begin{tabular}{l}
                    $s_{old} = s$\\
                    $n = n + 1$\\
                    $s_+=s+\delta$\\
                    $s_-=s-\delta$\\
                    $\tau'_+=shift(\tau', s_+)$\\
                    $\tau'_-=shift(\tau', s_-)$\\
                    $\mathbf{X_+} = LSQ(\tau_+, A)$\\
                    $\mathbf{X_-} = LSQ(\tau_-, A)$\\
                    $\mathbf{X} = \min(\mathbf{X_+}, \mathbf{X_-})$\\
                    $\chi^2 = chisq(\tau', X)$\\
                \end{tabular}
            \end{table}
        }; 

\end{tikzpicture}
\end{center}
\caption{}
\label{fig:}
\end{figure}

\end{document}
